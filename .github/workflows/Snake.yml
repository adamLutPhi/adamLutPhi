# GitHub Action for generating a contribution graph with a snake eating your contributions.

name: Generate Snake

# Controls when the action will run. This action runs every 6 hours.

on:
  schedule:
      # every 6 hours
    - cron: "0 */3 * * *"

# This command allows us to run the Action automatically from the Actions tab.
  workflow_dispatch:

# The sequence of runs in this workflow:
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    
    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
    #
    # Generates the snake  
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: adamwillisXanax
          Password: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
          #$ Username for 'https://github.com' : username
          #$ Password for 'https://github.com' : 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
                # these next 2 lines generate the files on a branch called "output". This keeps the main branch from cluttering up.
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

     # show the status of the build. Makes it easier for debugging (if there's any issues).
      #- run: git status

      # Push the changes
      #- name: Push changes
      #  uses: ad-m/github-push-action@master
      #  with:
      #    github_token: $sAakeAble_P0is.n
      #    branch: master
      #    force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: $sAakeAble_P0is.n
          #git remote set-url origin https://sAakeAble_P0is.n@github.com/adamwillisXanax/adamwillisXanax

          # these next 2 lines generate the files on a branch called "output". This keeps the main branch from cluttering up.
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg
          
     # show the status of the build. Makes it easier for debugging (if there's any issues).
      #- run: git status
          
      # Push the changes
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: $sAakeAble_P0is.n
          branch: master
          force: true
          
      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: $sAakeAble_P0is.n